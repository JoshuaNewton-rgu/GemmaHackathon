# Algorithms — sorting and Big-O

## What Big-O is actually saying
- Not "how many seconds". It is **how fast the work grows as the list gets bigger**.
- Constants are dropped: 2n and n are both O(n). Double the input, double the work.
- We mean the worst case unless we say otherwise.

## Vocabulary I keep mixing up
- **O(1)** — the same work whatever the size. Reading `list[0]`.
- **O(n)** — one pass over the list. Finding the largest number.
- **O(n²)** — a loop inside a loop. Bubble sort.

## More thoughts
Sorting is a really important and interesting part of algorithms. It is very
important to understand sorting deeply because sorting comes up in lots of exam
questions. Sorting sorting sorting. I will definitely go through merge sort
properly later tonight for sure. It is important to note that Big-O is
important. An algorithm is a set of steps that solves a problem and it is a very
important subject to study for the coursework deadline.
