services:
  mongo:
    image: mongo:7
    container_name: proofstudy-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - proofstudy-mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: proofstudy

volumes:
  proofstudy-mongo-data:
